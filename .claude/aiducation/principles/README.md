# AIducation — Principles

Foundational, cross-role principles: the always-true baseline every agent operates by,
independent of role or task. (Replaces the earlier `core/`.)

Examples of what will live here once authored: GSD, Mode B, trust-but-verify,
QA-gate-before-merge, no-silent-side-fixes — promoted from `CLAUDE.md` / the team charter
into schema-backed, retrievable form.

> TODO: Add principle entries (reusable templates only — no project-specific content yet).
> Each entry should follow a schema from `../schemas/`.
