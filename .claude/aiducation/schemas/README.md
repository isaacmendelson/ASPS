# AIducation — Schemas

Structured formats (YAML) for AIducation content. Every lesson, prompt, and role note
follows a schema here so entries stay consistent, indexable, and machine-readable.

| Schema | Describes |
|---|---|
| [lesson.schema.yaml](lesson.schema.yaml) | A lesson-learned entry |
| [prompt.schema.yaml](prompt.schema.yaml) | A reusable prompt template |

> TODO: Schemas are placeholders. Finalize fields before content is authored against them.
