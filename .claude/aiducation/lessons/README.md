# AIducation — Lessons

Lessons learned: a durable record of what went wrong or right and the takeaway, so the
team does not relearn the same thing twice.

> TODO: Add lessons (reusable template only — no project-specific content yet).
> Each lesson should follow `../schemas/lesson.schema.yaml`.
