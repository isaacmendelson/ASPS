---
phase: 06-repository-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore]
autonomous: true

must_haves:
  truths:
    - "Repository size reduced by at least 1.5GB"
    - "No duplicate ZIP files exist in working tree"
    - "No __pycache__ or .pyc files exist in working tree"
    - "No duplicate virtual environments exist (only .venv remains)"
    - "No .NET build artifacts exist (bin/, obj/, .vs/)"
    - "Comprehensive .gitignore prevents future bloat"
  artifacts:
    - path: ".gitignore"
      provides: "Comprehensive ignore patterns for Python, .NET, Node.js, and IDE artifacts"
      min_lines: 40
  key_links:
    - from: "git add/commit operations"
      to: ".gitignore rules"
      via: "git status filtering"
      pattern: "^[^#].*/"
---

<objective>
Clean repository of ~1.8GB bloat and prevent future accumulation.

Purpose: Remove duplicate ZIPs, build artifacts, Python caches, duplicate venvs, and temp files that have accumulated during development. Add comprehensive .gitignore to prevent recurrence.

Output: Clean repository with only necessary files, protected by .gitignore covering Python, .NET, Node.js, and IDE patterns.
</objective>

<execution_context>
@C:/Users/pc/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/pc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CLEANUP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete duplicate ZIPs and temp files</name>
  <files>
    apps.zip
    apps (2).zip
    apps (3).zip
    basic-url-analyzer/basic-url-analyzer.zip
    basic-url-analyzer/basic-url-analyzer (2) הכי חדש בלי הLLV .zip
    basic-url-analyzer/basic-url-analyzer (2)עם LLM.zip
    basic-url-analyzer/basic-url-analyzer/basic-url-analyzer.zip
    nul
    test_result.json
    test_result.txt
    copy_new_version.ps1
    extract_auth.ps1
    extract_hwid.ps1
    list_zip.ps1
    search_zip.ps1
    test_category.ps1
    update_emails.sql
  </files>
  <action>
Delete all duplicate ZIP files (7 files totaling ~1.13GB):
- Root: apps.zip, "apps (2).zip", "apps (3).zip" (3 duplicates of extracted apps/ directory)
- basic-url-analyzer/: basic-url-analyzer.zip, two Hebrew-named ZIPs (older versions)
- basic-url-analyzer/basic-url-analyzer/: basic-url-analyzer.zip (nested duplicate)

Delete temp files in root (9 files):
- nul, test_result.json, test_result.txt (test artifacts from test_category.ps1)
- 6 PowerShell scripts: copy_new_version.ps1, extract_auth.ps1, extract_hwid.ps1, list_zip.ps1, search_zip.ps1, test_category.ps1 (one-time debugging scripts)
- update_emails.sql (one-time test data script)

Use Windows-compatible paths with escaped spaces and Hebrew characters. Do NOT delete aspsbackend2db_20260130.sql (60MB database dump - out of scope).

Commands:
```powershell
# Delete root ZIPs
Remove-Item -Path "apps.zip" -Force -ErrorAction SilentlyContinue
Remove-Item -Path "apps (2).zip" -Force -ErrorAction SilentlyContinue
Remove-Item -Path "apps (3).zip" -Force -ErrorAction SilentlyContinue

# Delete basic-url-analyzer ZIPs
Remove-Item -Path "basic-url-analyzer\basic-url-analyzer.zip" -Force -ErrorAction SilentlyContinue
Remove-Item -Path "basic-url-analyzer\basic-url-analyzer (2) הכי חדש בלי הLLV .zip" -Force -ErrorAction SilentlyContinue
Remove-Item -Path "basic-url-analyzer\basic-url-analyzer (2)עם LLM.zip" -Force -ErrorAction SilentlyContinue
Remove-Item -Path "basic-url-analyzer\basic-url-analyzer\basic-url-analyzer.zip" -Force -ErrorAction SilentlyContinue

# Delete temp files and scripts
Remove-Item -Path "nul","test_result.json","test_result.txt" -Force -ErrorAction SilentlyContinue
Remove-Item -Path "copy_new_version.ps1","extract_auth.ps1","extract_hwid.ps1","list_zip.ps1","search_zip.ps1","test_category.ps1" -Force -ErrorAction SilentlyContinue
Remove-Item -Path "update_emails.sql" -Force -ErrorAction SilentlyContinue
```
  </action>
  <verify>
Check files deleted:
```powershell
# Verify ZIPs gone
Get-Item "apps*.zip" 2>$null
Get-Item "basic-url-analyzer\*.zip" 2>$null
Get-Item "basic-url-analyzer\basic-url-analyzer\*.zip" 2>$null

# Verify temp files gone
Get-Item "nul","test_result.*","*.ps1","update_emails.sql" 2>$null
```
All commands should return no files found.
  </verify>
  <done>
All 7 duplicate ZIPs deleted (~1.13GB freed).
All 9 temp files and scripts deleted from root.
aspsbackend2db_20260130.sql remains (intentionally preserved).
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete build artifacts and duplicate venvs</name>
  <files>
    ASPSBackend14_J/.vs/
    ASPSBackend14_J/**/bin/
    ASPSBackend14_J/**/obj/
    WebApi/publish/
    basic-url-analyzer/basic-url-analyzer/basic-url-analyzer/venv/
    basic-url-analyzer/basic-url-analyzer2/venv/
    **/__pycache__/
    **/.pytest_cache/
  </files>
  <action>
Delete .NET build artifacts (~100MB):
- ASPSBackend14_J/.vs/ (28MB Visual Studio cache)
- All bin/ and obj/ directories in ASPSBackend14_J (~50MB)
- WebApi/publish/ in root (19MB orphaned build output)

Delete duplicate virtual environments (~450MB):
- Keep .venv, delete venv in basic-url-analyzer/basic-url-analyzer/basic-url-analyzer/
- Keep .venv, delete venv in basic-url-analyzer/basic-url-analyzer2/

Delete Python artifacts (~100MB):
- All __pycache__/ directories (1,679 directories across project)
- All .pytest_cache/ directories

Commands:
```powershell
# .NET artifacts
Remove-Item -Path "ASPSBackend14_J\.vs" -Recurse -Force -ErrorAction SilentlyContinue
Get-ChildItem -Path "ASPSBackend14_J" -Recurse -Directory -Filter "bin" | Remove-Item -Recurse -Force -ErrorAction SilentlyContinue
Get-ChildItem -Path "ASPSBackend14_J" -Recurse -Directory -Filter "obj" | Remove-Item -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item -Path "WebApi\publish" -Recurse -Force -ErrorAction SilentlyContinue

# Duplicate venvs (keep .venv, delete venv)
Remove-Item -Path "basic-url-analyzer\basic-url-analyzer\basic-url-analyzer\venv" -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item -Path "basic-url-analyzer\basic-url-analyzer2\venv" -Recurse -Force -ErrorAction SilentlyContinue

# Python artifacts
Get-ChildItem -Path . -Recurse -Directory -Filter "__pycache__" | Remove-Item -Recurse -Force -ErrorAction SilentlyContinue
Get-ChildItem -Path . -Recurse -Directory -Filter ".pytest_cache" | Remove-Item -Recurse -Force -ErrorAction SilentlyContinue
```
  </action>
  <verify>
Check artifacts deleted:
```powershell
# Verify .NET artifacts gone
Test-Path "ASPSBackend14_J\.vs"
Test-Path "WebApi\publish"
(Get-ChildItem -Path "ASPSBackend14_J" -Recurse -Directory -Filter "bin" -ErrorAction SilentlyContinue).Count
(Get-ChildItem -Path "ASPSBackend14_J" -Recurse -Directory -Filter "obj" -ErrorAction SilentlyContinue).Count

# Verify duplicate venvs gone (but .venv remains)
Test-Path "basic-url-analyzer\basic-url-analyzer\basic-url-analyzer\venv"
Test-Path "basic-url-analyzer\basic-url-analyzer\basic-url-analyzer\.venv"
Test-Path "basic-url-analyzer\basic-url-analyzer2\venv"
Test-Path "basic-url-analyzer\basic-url-analyzer2\.venv"

# Verify Python artifacts gone
(Get-ChildItem -Path . -Recurse -Directory -Filter "__pycache__" -ErrorAction SilentlyContinue).Count
(Get-ChildItem -Path . -Recurse -Directory -Filter ".pytest_cache" -ErrorAction SilentlyContinue).Count
```
Expected: All Test-Path commands return False, all counts return 0, except .venv paths return True.
  </verify>
  <done>
ASPSBackend14_J/.vs/, all bin/obj directories, and WebApi/publish/ deleted.
Duplicate venv/ directories deleted, .venv directories preserved.
All __pycache__/ and .pytest_cache/ directories deleted.
~650MB freed (100MB .NET + 450MB venvs + 100MB Python caches).
  </done>
</task>

<task type="auto">
  <name>Task 3: Create comprehensive .gitignore</name>
  <files>.gitignore</files>
  <action>
Create root .gitignore covering all artifact types found in cleanup analysis.

Include patterns for:
- Python: __pycache__/, *.pyc, .venv/, venv/, .pytest_cache/, *.egg-info/
- .NET: bin/, obj/, .vs/, *.user, *.suo, publish/
- Node.js: node_modules/, dist/, .next/, build/
- IDEs: .idea/, .vscode/, *.swp, *~
- OS: .DS_Store, Thumbs.db, nul
- Project-specific: *.zip, *.log, cache/, test_result.*

Do NOT add *.sql to ignore patterns (database dumps like aspsbackend2db_20260130.sql may be intentional).

If .gitignore already exists, read it first and merge patterns (don't overwrite existing custom rules).
  </action>
  <verify>
Check .gitignore created and contains required patterns:
```powershell
Get-Content .gitignore | Select-String -Pattern "__pycache__"
Get-Content .gitignore | Select-String -Pattern "bin/"
Get-Content .gitignore | Select-String -Pattern "node_modules/"
Get-Content .gitignore | Select-String -Pattern ".idea/"
Get-Content .gitignore | Select-String -Pattern "*.zip"
```
All patterns should be found.

Verify no *.sql pattern added:
```powershell
Get-Content .gitignore | Select-String -Pattern "^\*\.sql"
```
Should return no matches.
  </verify>
  <done>
.gitignore exists with comprehensive patterns covering Python, .NET, Node.js, IDE, OS, and project-specific artifacts.
No *.sql pattern added (preserves intentional database dumps).
Future bloat prevented by ignore rules.
  </done>
</task>

</tasks>

<verification>
Final repository health check:

1. **Size reduction verification:**
   ```powershell
   # Requires du utility or PowerShell equivalent
   # Target: at least 1.5GB reduction
   ```

2. **git status cleanliness:**
   ```bash
   git status --untracked-files
   ```
   Should show only intentional untracked files (not __pycache__, bin/, obj/, etc.)

3. **No bloat patterns remain:**
   ```powershell
   # No duplicate ZIPs
   Get-ChildItem -Path . -Recurse -Filter "*.zip" | Where-Object { $_.Name -match "apps|basic-url-analyzer" }

   # No __pycache__
   Get-ChildItem -Path . -Recurse -Directory -Filter "__pycache__"

   # No duplicate venvs (venv/ should be gone, .venv should exist)
   Get-ChildItem -Path . -Recurse -Directory -Filter "venv" | Where-Object { $_.Name -eq "venv" }

   # No .NET bin/obj in ASPSBackend14_J
   Get-ChildItem -Path "ASPSBackend14_J" -Recurse -Directory -Filter "bin"
   Get-ChildItem -Path "ASPSBackend14_J" -Recurse -Directory -Filter "obj"
   ```
   All queries should return zero results.

4. **.gitignore coverage:**
   ```bash
   cat .gitignore
   ```
   Should contain patterns for Python, .NET, Node.js, IDE, and OS artifacts.
</verification>

<success_criteria>
Repository cleanup successful when:

1. Repository size reduced by at least 1.5GB (verified by disk usage comparison before/after)
2. All 7 duplicate ZIP files deleted (apps.zip x3, basic-url-analyzer ZIPs x4)
3. All __pycache__ and .pytest_cache directories removed (zero matches in recursive search)
4. Duplicate venv/ directories removed, .venv directories preserved
5. All .NET build artifacts removed (ASPSBackend14_J/.vs/, bin/, obj/, WebApi/publish/)
6. All temp files and PowerShell scripts removed from root
7. Comprehensive .gitignore exists with 40+ lines covering Python, .NET, Node.js, IDE, OS patterns
8. git status shows clean working tree with only intentional untracked files
9. aspsbackend2db_20260130.sql preserved (60MB database dump intentionally kept)
10. No restructuring performed (basic-url-analyzer nesting unchanged - out of scope)
</success_criteria>

<output>
After completion, create `.planning/phases/06-repository-cleanup/06-01-SUMMARY.md`
</output>
