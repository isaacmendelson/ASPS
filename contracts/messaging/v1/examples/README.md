# ASPS messaging v1 examples

The canonical executable examples are the JSON documents under `../fixtures/`.
The contract test runner validates each document in C#, Python, JavaScript, and
the Basic URL Analyzer generated binding.
