# AntiScam Desktop App - direct dependencies.
# Exact transitive resolution is committed in requirements.lock.txt.

# Environment variable loading
python-dotenv==1.2.2

# WebSocket for extension communication
websockets==16.1.1

# ZeroMQ for backend communication
pyzmq==27.1.0

# HTTP requests (for Google Auth)
requests==2.34.2

# System tray
pystray==0.19.5
Pillow==12.3.0

# Modern tkinter widgets for popup UI
customtkinter==6.0.0

# Windows native toast notifications (used by notification_manager.py)
winotify==1.1.0

# Process monitoring
psutil==7.2.2

# IP Geolocation (optional - gracefully degrades if missing)
geoip2==5.3.0

# For building EXE
pyinstaller==6.21.0

# Secure credential storage (Windows Credential Manager / macOS Keychain / libsecret)
keyring==25.7.0

# Test runner
pytest==9.1.1
