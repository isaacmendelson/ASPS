# AIducation — Prompts

Reusable prompt templates: vetted prompts the team can reuse instead of reinventing them
per task.

> TODO: Add prompt templates (reusable only — no project-specific content yet).
> Each prompt should follow `../schemas/prompt.schema.yaml`.
